<div class="row">
	<%= render 'left_sidebar' %>
	<div class="col s7">
		<ul class="collection">
			<%= render @users %>
			<%= will_paginate @users %>
		</ul>
	</div>
	<%= render 'static_pages/right_sidebar' %>
</div>