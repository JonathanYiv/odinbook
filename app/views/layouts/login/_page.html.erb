<%= render 'layouts/login/header' %>

<% flash.each do |key, value| %>
  <%= content_tag :div, value, class: "flash #{key}" %>
<% end %>

<main class="facebook-grey">
    <div class="center-align container">
        <%= yield %>
    </div>
</main>